<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>{% block title %}Acteurs{% endblock %}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  {# === CSS embebido: NO usa /public/css/app.css === #}
  <style>
    :root{
      --bg:#0e0d0a; --panel:#151311; --ink:#f0e9dc; --muted:#c2b9a3;
      --accent:#c49a6c; --ok:#1fa971; --warn:#cc884a; --danger:#d14d4d;
      --co-yellow:#f4d03f; --co-blue:#3b6fb6; --co-red:#d94a4a;
      --br:12px; --pad:14px;
    }
    *{box-sizing:border-box}
    html,body{margin:0;background:var(--bg);color:var(--ink);
      font-family:system-ui,Segoe UI,Roboto,Arial}
    header,footer{padding:20px 16px;border-bottom:1px solid #222}
    footer{border:none;opacity:.75}
    main{max-width:1100px;margin:18px auto;padding:0 16px}
    h1{margin:0 0 4px;font-size:28px}
    h2{margin:22px 0 10px;font-size:20px}
    .muted{color:var(--muted)}

    .toolbar{display:flex;gap:12px;align-items:center;flex-wrap:wrap;margin-bottom:14px}
    .search{display:flex;gap:8px;align-items:center}
    .search input{width:140px}

    a{color:#ffd57a;text-decoration:none}
    nav a{color:#ffd57a;text-decoration:none;margin-right:12px}
    nav a:hover{text-decoration:underline}

    .btn{
      border:1px solid #2a2723;background:#1a1816;color:var(--ink);
      padding:8px 12px;border-radius:999px;cursor:pointer;display:inline-block
    }
    .btn:hover{border-color:var(--accent)}
    .btn.on{background:var(--ok);border-color:transparent}
    .btn.muted{opacity:.85}
    .btn.danger{background:var(--danger);border-color:transparent}

    .grid{width:100%;border-collapse:collapse;background:var(--panel);
      border:1px solid #26231f;border-radius:var(--br);overflow:hidden}
    .grid thead th{background:#1b1916}
    .grid th,.grid td{padding:10px;border-bottom:1px solid #26231f;vertical-align:top}
    .grid img{width:60px;height:60px;object-fit:cover;border-radius:8px;border:1px solid #2a2723}

    .panel{background:var(--panel);border:1px solid #26231f;border-radius:var(--br);padding:var(--pad);margin-top:16px}
    .row{display:grid;grid-template-columns:160px 1fr;gap:10px;align-items:start;margin-bottom:12px}
    input,textarea{
      width:100%;padding:10px;background:#12100e;color:var(--ink);
      border:1px solid #2a2723;border-radius:8px
    }
    input:focus,textarea:focus{outline:1px solid var(--co-yellow);border-color:var(--co-blue)}
    .actions{display:flex;gap:10px;align-items:center}

    .badge{display:inline-block;padding:2px 8px;border-radius:999px;font-size:12px;border:1px solid #2a2723}
    .badge.co{background:linear-gradient(90deg,var(--co-yellow),var(--co-blue),var(--co-red));color:#111}

    #flash{margin:8px 0 12px}
    .alert{padding:10px;border-radius:8px}
    .alert.ok{background:#112418;border:1px solid #23543a}
    .alert.err{background:#2a1212;border:1px solid #5c2a2a}

    /* Cards (show) */
    .card{display:flex;gap:16px;align-items:flex-start;background:var(--panel);
      border:1px solid #26231f;border-radius:var(--br);padding:var(--pad)}
    .thumb-lg{width:160px;height:160px;object-fit:cover;border-radius:12px;border:1px solid #2a2723}
    .row { margin-bottom: 8px; }
  </style>

  
</head>
<body>
<header>
  <h1> Acteurs</h1>
  <nav>
    <a href="{{ path('acteurs_index') }}">Liste des acteurs</a>
    <a href="{{ path('acteurs_ajouter') }}" class="on">Ajouter</a>
  </nav>
  <p class="muted">Consommation de l’API FastAPI (JSON file)</p>
</header>

<main>
  {% for m in app.flashes('success') %}
    <div class="alert ok">{{ m }}</div>
  {% endfor %}
  {% for m in app.flashes('error') %}
    <div class="alert err">{{ m }}</div>
  {% endfor %}

  {% block content %}{% endblock %}
</main>

<footer><small class="muted">Symfony</small></footer>
</body>
</html>
